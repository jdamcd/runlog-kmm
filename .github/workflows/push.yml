name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    name: Build and test
    runs-on: macos-12
    steps:

    - uses: actions/checkout@v3

    - name: Setup JDK
      uses: actions/setup-java@v3
      with:
       distribution: 'adopt'
       java-version: '11'

    - name: Assemble
      run: ./gradlew spotlessCheck assembleDebug

    - name: Run Shared and Android tests
      run: ./gradlew testDebugUnitTest

    - name: Run iOS tests
      run: xcodebuild test -project ios/RunLog.xcodeproj -scheme RunLog -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 12,OS=16.0' -resultBundlePath TestResults

    - name: Publish Kotlin results
      uses: dorny/test-reporter@v1.6.0
      if: always()
      with:
        name: Kotlin results
        path: "**/build/test-results/**/TEST-*.xml"
        reporter: java-junit
        fail-on-error: true

    - name: Publish iOS results
      uses: kishikawakatsumi/xcresulttool@v1.7.0
      if: always()
      with:
        name: iOS results
        path: TestResults.xcresult
        fail-on-error: true
